[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Container WORKDIR is /app/service (see Dockerfile)
preDeployCommand = ["alembic", "upgrade", "head"]
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
